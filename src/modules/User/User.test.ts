describe("Account creation", () => {
  it("should be able to create an account with valid data", async () => {
    const name = "John Doe";
    const email = "john@gmail.com";
    const password = "123456";
    //expect(user.id).toBeGreaterThanOrEqual(1);
  });
});
